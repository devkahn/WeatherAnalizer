<UserControl x:Class="WeatherAnalizer.Views.Pages.ucDataWeatherByStation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherAnalizer.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid>
        <DataGrid.Style>
            <Style TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="CanUserReorderColumns" Value="False"/>
                <Setter Property="CanUserResizeColumns" Value="False"/>
                <Setter Property="CanUserResizeRows" Value="False"/>
                <Setter Property="CanUserSortColumns" Value="False"/>
                <Setter Property="ColumnHeaderHeight" Value="27"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource color_Gray2}"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="RowHeight" Value="35"/>
            </Style>
        </DataGrid.Style>
        <DataGrid.Resources>
            <Style TargetType="DataGridCell">
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </DataGrid.Resources>
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="급지"/>
            <DataGridTemplateColumn Header="관측소"/>
            <DataGridTemplateColumn Header="주소"/>
            <DataGridTemplateColumn Header="거리"/>
            <DataGridTemplateColumn Header="무더위"/>
            <DataGridTemplateColumn Header="강우"/>
            <DataGridTemplateColumn Header="강추위"/>
            <DataGridTemplateColumn Header="강설"/>
            <DataGridTemplateColumn Header="강풍"/>
            <DataGridTemplateColumn>
                <DataGridTemplateColumn.Header>
                    <Border Width="220" Background="{StaticResource color_Primary}" Height="35">
                        
                    </Border>
                </DataGridTemplateColumn.Header>
            </DataGridTemplateColumn>

        </DataGrid.Columns>
        1
    </DataGrid>
</UserControl>
